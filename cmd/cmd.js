function prefixHandler(msg, prefix, cmd, args) {
    if (cmd === `${prefix}mds`) {
        // do MDS roll logic
    }
    else if (cmd === `${prefix}roll` || cmd === `${prefix}r`) {
        // do basic roll logic
    }
}